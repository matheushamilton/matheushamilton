name: Generate Snake

on:
  schedule:
    # Executa automaticamente a cada 24 horas
    - cron: "0 0 * * *"
  
  # Permite rodar manualmente a qualquer momento pela aba Actions
  workflow_dispatch:
  
  # Executa sempre que houver um push na branch main
  push:
    branches:
    - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      # Gera o jogo da cobrinha a partir do gráfico de contribuições do matheushamilton
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: matheushamilton
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?color_snake=#00FF66&color_dots=#161b22,#0e4429,#006d32,#26a641,#39d353

      # Envia os arquivos gerados para a branch "output"
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
